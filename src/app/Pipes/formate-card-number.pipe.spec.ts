import { FormateCardNumberPipe } from './formate-card-number.pipe';

describe('FormateCardNumberPipe', () => {
  it('create an instance', () => {
    const pipe = new FormateCardNumberPipe();
    expect(pipe).toBeTruthy();
  });
});
